import { Geist, Forum } from "next/font/google";

export const mainFont = Geist({
  weight: "400",
  subsets: ["latin"],
});

export const titleFont = Forum({
  weight: "400",
  subsets: ["latin"],
});
